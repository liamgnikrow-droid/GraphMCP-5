---
uid: "DOMAIN-Internal_Tools_Registry"
title: "Реестр внутренних утилит (Shadow Scripts)"
type: "Domain"
project_id: "graphmcp"
status: "Active"
tags: [graph/domain, state/active]
cssclasses: [juggl-node, type-domain, premium-card]
---
# Реестр внутренних утилит (Shadow Scripts)

> [!abstract] Domain Context
> **ID:** `DOMAIN-Internal_Tools_Registry` | **Status:** `Active`

## Description
## 1. Введение

В архитектуре **Iron Dome** существует четкое разделение инструментов:
1.  **User Space (MCP Tools):** Стандартизированные, безопасные инструменты, доступные Агенту (описаны в `server.py` и Мета-Графе).
2.  **Kernel Space / Admin Tools (Shadow Scripts):** Прямые скрипты на Python, имеющие неограниченный доступ к базе данных и файловой системе. Используются для "хирургических" операций, починки архитектуры и инициализации.

## 2. Категории Скриптов

Ниже приведен полный реестр скриптов, расположенных в директории `Tools/`, сгруппированных по назначению.

### 🛠️ 2.1. Maintenance (Обслуживание и Ремонт)
*Скрипты для ручного исправления аномалий, нарушений иерархии и очистки мусора.*

| Файл | Описание | Критичность |
| :--- | :--- | :--- |
| `maintenance/fix_hierarchy.py` | **HOT**. Исправляет нарушения иерархии (например, переносит Requirements от Idea к Spec). Обновляет связи в БД и синхронизирует все затронутые файлы. | 🔴 Высокая |
| `maintenance/find_orphan_files.py` | Поиск "файловых сирот" — Markdown-файлов, для которых нет соответствующих узлов в Neo4j. | 🟢 Низкая |
| `maintenance/purge_ignored_nodes.py` | Очистка графа от узлов, помеченных тегом `ignored` или являющихся мусором сканирования. | 🟢 Низкая |
| `maintenance/cleanup_obsolete_files.py` | Удаление устаревших файлов из `Graph_Export`, которые более не актуальны в графе. | 🟡 Средняя |
| `hard_reset_duplicates.py` | Экстренный инструмент для удаления дубликатов контента в файлах (например, при ошибках `MERGE` в GraphSync). | 🔴 Высокая |
| `maintenance/delete_duplicate_functions.py` | Удаление дубликатов функций в графе кода (Code Nodes). | 🟡 Средняя |

### 🏗️ 2.2. Bootstrap & Setup (Инициализация)
*Скрипты, запускаемые единоразово при развертывании проекта или обновлении ядра.*

| Файл | Описание | Критичность |
| :--- | :--- | :--- |
| `bootstrap_metagraph.py` | **CORE**. Загружает "Законы Физики" (Meta-Graph: Action, Constraint, NodeType) в Neo4j. Без этого Agent слеп. | 🔴🔴 Критичная |
| `import_md_to_neo4j.py` | Импортер. Сканирует папку `Graph_Export` и воссоздает граф в Neo4j на основе Markdown-файлов. | 🔴 Высокая |
| `maintenance/link_physics_implementation.py` | Связывает абстрактные узлы `:Action` в графе с реальными функциями Python (`server.py`) через связь `IMPLEMENTS`. | 🔴 Высокая |
| `update_physics_ru.py` | Утилита локализации. Обновляет тексты законов и ограничений на русский язык. | 🟢 Низкая |

### 🧪 2.3. Diagnostic & Testing (Диагностика)
*Скрипты для проверки работоспособности системы в обход стандартных механизмов MCP.*

| Файл | Описание |
| :--- | :--- |
| `test_find_orphans.py` | Стандартный тест инструмента `find_orphans`. |
| `test_middleware.py` | Тестирование "Линзы" (Middleware). Проверяет, правильно ли скрываются/показываются инструменты в зависимости от контекста. |
| `diagnostic.py` | Базовая проверка здоровья: подключение к Neo4j, наличие драйвера. |
| `test_format_cypher.py` | Тестирование генератора Cypher-запросов. |

### 📚 2.4. Core Libraries (Ядро)
*Общие модули, используемые как сервером, так и скриптами обслуживания.*

| Файл | Описание |
| :--- | :--- |
| `graph_sync.py` | **ENGINE**. Движок двусторонней синхронизации Neo4j ↔ Markdown/Obsidian. Центральный компонент persistence layer. |
| `db_config.py` | Единая точка конфигурации подключения к базе данных (URI, Auth, Drivers). |
| `codebase_mapper.py` | Логика статического анализа кода (AST) для построения графа кода (File -> Class -> Function). |

## 3. Рекомендации по использованию

1.  **Только Human:** Запуск скриптов из папки `Tools/maintenance/` должен производиться только оператором через терминал Docker контейнера.
2.  **Backup first:** Перед запуском деструктивных скриптов (`hard_reset`, `purge`) рекомендуется делать бэкап базы или коммит в git.
3.  **Контекст Docker:** Скрипты должны запускаться внутри контейнера `graphmcp-core` для корректного доступа к сети Docker (Neo4j) и томам.
    ```bash
    docker-compose exec -T -e PYTHONIOENCODING=utf-8 graphmcp-core python /workspace/Tools/maintenance/<script_name>.py
    ```

---
*Документ автоматически сгенерирован на основе анализа файловой структуры проекта.*
