# GraphMCP-5 Roadmap

> Дорожная карта развития Graph-Native Agent Architecture

---

## ✅ Фаза 1: Фундамент (ЗАВЕРШЕНО)

### 1.1 Мета-Граф
- [x] Спецификация SPEC-Graph_Physics.md
- [x] Bootstrap скрипт для инициализации Мета-Графа
- [x] NodeTypes: Idea, Spec, Requirement, Task, Domain
- [x] Actions с scope (global/contextual)
- [x] Constraints: Russian Language, No WikiLinks, One Spec

### 1.2 MCP Server Core
- [x] Базовые инструменты: look_around, move_to, create_concept
- [x] Middleware для проверки доступности инструментов
- [x] Динамическая загрузка правил из Мета-Графа
- [x] GraphSync для синхронизации Neo4j → Obsidian

### 1.3 Семантика
- [x] Эмбеддинги (all-MiniLM-L6-v2)
- [x] look_for_similar — семантический поиск
- [x] Автоматическое сохранение эмбеддингов при создании нод

---

## ✅ Фаза 2: Расширенные инструменты (ЗАВЕРШЕНО)

### 2.1 Управление графом
- [x] link_nodes — создание связей между нодами
- [x] delete_node — удаление нод (с проверкой структурной целостности)
- [x] delete_link — удаление связей (с защитой от островов)
- [x] sync_graph — принудительная синхронизация

### 2.2 Инструменты агента
- [x] register_task — регистрация задач от Human
- [x] explain_physics — интроспекция правил Мета-Графа
- [x] propose_change → **format_cypher** (реализовано в Фазе 4)

### 2.3 Надежность (CRITICAL)
- [x] Safe GraphSync — защита контента при синхронизации (merge strategy)
- [x] Conflict Detection — автоматическое создание Bug при конфликте версий

---

## ✅ Фаза 3: Middleware для Constraints (ЗАВЕРШЕНО)

### 3.1 Проверка Constraints перед действием
- [x] Функция `check_constraints()` в middleware
- [x] Загрузка Constraints из Мета-Графа (RESTRICTS связи)
- [x] Вызов Python примитивов:
  - `cyrillic_ratio(text)` — проверка языка
  - `regex_match(text, pattern)` — проверка WikiLinks
  - `node_count(label)` — проверка кардинальности (One Spec)

### 3.2 Интеграция в call_tool()
- [x] Проверять Constraints ПОСЛЕ middleware (доступность) и ПЕРЕД выполнением
- [x] Возвращать понятные ошибки с указанием нарушенного Constraint
- [x] Интегрировано в `tool_create_concept()` и `tool_register_task()`

---

## ✅ Фаза 4: Контекстуальный Интеллект (ЗАВЕРШЕНО)

> **Цель:** Агент "видит" весь релевантный контекст одним взглядом

### 4.1 get_full_context — "Покажи мне всё"
- [x] Новый глобальный инструмент
- [x] Входные данные: query (текст запроса/задачи)
- [x] Выходные данные:
  - Семантически близкие ноды (top-5 по эмбеддингам)
  - Связанные Specs и Requirements (по графу)
  - Активные Constraints (что применится)
  - Текущая локация и соседи
  - Impact preview (что затронет действие)

### 4.2 Impact Analysis в create_concept
- [x] Перед созданием ноды показывать:
  - Какие ноды будут связаны автоматически
  - Какие Constraints применятся
  - Семантически близкие существующие ноды (возможные дубликаты)
- [x] Опциональное подтверждение перед созданием

### 4.3 Переименование propose_change → format_cypher
- [x] Убрать "предложение" — агент не предлагает архитектурные изменения
- [x] Инструмент только форматирует Cypher по запросу Human
- [x] Убрать автоматическое создание Proposal нод

---

## 🔮 Фаза 5: Связь с кодом

> **Цель:** Граф знает о реальном коде проекта

### 5.1 map_codebase — сканирование кода
- [ ] Создание File нод для .py, .ts, .js файлов
- [ ] Создание Function/Class нод для ключевых элементов
- [ ] Автоматические эмбеддинги для кода

### 5.2 Связи Code → Requirements
- [ ] IMPLEMENTS связь: Function → Requirement
- [ ] DEPENDS_ON связь: File → File (импорты)
- [ ] Агент может спросить "Какой код реализует REQ-Auth?"

### 5.3 Code Context в get_full_context
- [ ] Показывать связанные файлы и функции
- [ ] "Это изменение затронет: auth.py, user_service.py"

---

## ✅ Фаза 6: Чтение контента нод (ЗАВЕРШЕНО)

> **Цель:** Агент понимает содержимое нод, а не только их структуру

### 6.1 read_node — чтение полного контента
- [x] Глобальный инструмент для чтения любой ноды по UID
- [x] Читает из Neo4j content property
- [x] Fallback на Markdown файл если DB content пустой
- [x] Ограничение вывода (8000 chars) для защиты от token explosion

### 6.2 Summarize Area (опционально)
- [ ] `summarize_area(uid, depth)` — собирает контент нод в радиусе N шагов
- [ ] Возвращает агенту для анализа (агент сам делает summary)

> **Примечание:** RAG-слой признан избыточным (LLM внутри LLM). 
> Агент сам является LLM и может анализировать контент через read_node.

---

## ✅ Фаза 7: Контекстуальная Навигация (ЗАВЕРШЕНО)

> **Цель:** Агент — "человек с фонариком" который видит свою тропинку

### 7.1 Enhanced look_around — "Dashboard"
- [x] 📍 Current Location + description
- [x] 👥 Neighbors с типами связей (→/←) и описаниями
- [x] 🔧 Available Actions из Мета-Графа
- [x] ⚠️ Active Constraints (системные ограничения)
- [x] 📋 Related Requirements (в радиусе 2 hops) с описаниями
- [x] 📊 Graph Stats (статистика по типам нод)

### 7.2 move_to с auto-refresh
- [x] После перемещения автоматически возвращать look_around
- [x] "Камера следует за игроком" — контекст всегда актуален
- [x] Агенту не нужно отдельно вызывать look_around после move_to

### 7.3 illuminate_path — "Подсветка тропинки"
- [x] 🔦 Семантический поиск → точка входа
- [x] ⬆️ Traverse UP → до корня (Idea)
- [x] ⬇️ Traverse DOWN → до листьев (Task)
- [x] ↔️ Lateral connections (DEPENDS_ON, CONFLICT, IMPLEMENTS)
- [x] 📖 Return CONTENT всех нод на пути

> **Метафора:** Агент в тёмном лесу. illuminate_path подсвечивает 
> всю тропинку релевантную задаче, а не весь лес.

---

---

## 🔄 Фаза 8: Multi-Project Architecture (В РАБОТЕ)

> **Критическая проблема:** Сейчас мета-граф (законы) и данные проекта смешаны в одной БД.  
> **Решение:** Разделить на Core (неизменные правила) и Project (данные конкретного проекта).

### 8.1 Разделение Core и Project данных

**Текущая ситуация:**
```
neo4j (одна БД) = мета-граф + данные GraphMCP-5
  ├── :NodeType, :Action, :Constraint  ← CORE (законы)
  └── :Idea, :Spec, :Requirement       ← PROJECT (данные)
```

**Целевая архитектура:**
```
core (БД)                    project_X (БД)
├── :NodeType                ├── :Idea
├── :Action                  ├── :Spec
├── :Constraint              ├── :Requirement
└── :Agent                   ├── :Task
                             └── :File
```

### 8.2 Реализация

- [ ] **8.2.1** Создать БД `core` в Neo4j
- [ ] **8.2.2** Перенести мета-граф (NodeType, Action, Constraint) в `core`
- [ ] **8.2.3** Модифицировать server.py:
  - [ ] `CORE_DB = "core"` (константа)
  - [ ] `PROJECT_DB` из .env или switch_project
  - [ ] Все запросы указывают правильную БД
- [ ] **8.2.4** Обновить bootstrap_metagraph.py для работы с `core`

### 8.3 Инструменты управления проектами

- [ ] **8.3.1** `switch_project(name, root)`
  - Переключает контекст на другой проект
  - Проверяет существование БД, создаёт если нужно
  - Обновляет PROJECT_DB и PROJECT_ROOT
  
- [ ] **8.3.2** `bootstrap_project(name, root, mode)`
  - Создаёт новый проект с БД
  - Инициализирует IDEA-Genesis
  - Устанавливает workflow режим
  
- [ ] **8.3.3** `list_projects()`
  - Показывает все доступные проекты
  - Текущий проект отмечается

### 8.4 Workflows (В РАБОТЕ)

Внедрение режимов работы агента для фокусировки на конкретных задачах и ограничения опасных действий.

**Режимы:**
1. **Architect (Архитектор):** Работа с графом (Idea, Spec, Req). ❌ Код писать нельзя.
2. **Builder (Строитель):** Реализация задач. ✅ Код разрешен. Требует привязки к Task.
3. **Auditor (Аудитор):** Анализ существующего кода. ✅ Чтение кода. ⚠️ Правки только через Task.

**Задачи:**
- [ ] **8.4.1** Добавить `set_workflow(mode)` инструмент
- [ ] **8.4.2** Реализовать фильтрацию инструментов в Middleware (`get_allowed_tool_names`)
- [ ] **8.4.3** Сохранять состояние Workflow в `.active_project_state`
- [ ] **8.4.4** Обновить SPEC-Graph_Physics правилами доступа

---

## 📋 Технический долг

### Исправления
- [ ] Убрать дублирование transliterate() (вынести в utils)
- [ ] Добавить логирование действий агента
- [ ] Оптимизировать запросы к Neo4j (индексы)

### Тестирование
- [ ] Интеграционные тесты для всех инструментов
- [ ] Тест с реальным агентом (MCP клиент)
- [ ] Нагрузочное тестирование (много нод)

### Документация
- [ ] README.md с инструкцией по запуску
- [ ] Примеры использования каждого инструмента
- [ ] Архитектурная диаграмма

---

## 📅 Приоритеты

| Приоритет | Задача | Оценка |
|:---:|:---|:---:|
| ✅ DONE | Фаза 3: Middleware для Constraints | 40-60 мин |
| ✅ DONE | Фаза 4.1: get_full_context | 30-40 мин |
| ✅ DONE | Фаза 4.2: Impact Analysis | 20-30 мин |
| ✅ DONE | Фаза 4.3: Переименовать propose_change | 15 мин |
| ✅ DONE | Фаза 6: read_node | 20 мин |
| ✅ DONE | Фаза 7: Контекстуальная Навигация | 45 мин |
| ✅ DONE | **Фаза 8.1: Multi-Project Foundation** | 1 час |
| 🔴 P0  | **Фаза 8.4: Workflows** | **45 мин** |
| 🟢 P1  | Фаза 5: Связь с кодом | 2-4 часа |

---

## 🎯 Ближайшая цель

**Идеальный flow агента:**

```
1. Human: "Добавь авторизацию"

2. Agent вызывает: illuminate_path("авторизация")
   → Видит ВСЮ тропинку: SPEC-Security → REQ-Auth → связанные Tasks
   → Получает контент всех нод на пути

3. Agent вызывает: move_to("REQ-Auth")
   → Автоматически получает полный dashboard новой локации
   → Видит: соседей, доступные действия, constraints

4. Agent вызывает: register_task("Реализовать 2FA", "OAuth2 + SMS")
   → Constraints проверяются автоматически
   → Видит Impact: "Затронет REQ-Auth, связан со SPEC-Security"

5. Agent вызывает: link_nodes(TASK → REQ-Auth)

6. Agent: "Задача создана и связана. Контекст:
           - Связанный Requirement требует OAuth2
           - Spec указывает на использование JWT
           - Constraints требуют русский язык
           Хотите чтобы я начал реализацию?"
```

---

*Последнее обновление: 2026-01-29*
