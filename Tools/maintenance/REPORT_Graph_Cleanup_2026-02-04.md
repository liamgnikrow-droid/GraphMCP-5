# Отчёт: Очистка графа от мусорных узлов (Tests & Maintenance)

**Дата:** 2026-02-04  
**Задача:** Устранить визуальный шум ("острова") в графе, вызванный индексацией вспомогательных файлов.

## Проблема

После запуска `map_codebase` в графе появилось множество изолированных узлов (островов), что создало впечатление "сломанного" графа.
Диагностика показала, что это были не критические файлы, а:
- Тесты (`test_*.py`)
- Скрипты обслуживания (`Tools/maintenance/*.py`)
- Документация (`md/*.md`)

Эти файлы не имеют связей `IMPLEMENTS` к Requirements, поэтому они выглядели как острова.

## Решение

### 1. Модификация CodebaseMapper

В `Tools/codebase_mapper.py` добавлен метод `_should_ignore`, который фильтрует файлы перед добавлением в граф.

**Игнорируются:**
- Директории: `maintenance`, `archive`, `Graph_Export`, `md`, `examples`
- Файлы: `test_*.py`, `*_test.py`, `mock_*.py`
- Логи: `*.log`

### 2. Очистка Графа

Запущен скрипт `purge_ignored_nodes.py`, который удалил узлы, попадающие под новые правила фильтрации.

**Статистика удаления:**
- 🗑️ Удалено File узлов: **31** (плюс все их Classes и Functions)
- 📄 Удалено Markdown файлов: **93**

### 3. Результат

- **Острова:** 0 (полностью устранены)
- **Целостность:** Основные файлы (`server.py`, `graph_sync.py`) остались нетронутыми и связанными.
- **Визуализация:** Граф в Juggl/Obsidian теперь чист от шума.

## Рекомендации

При создании новых инструментов или тестов:
- Если файл не является частью бизнес-логики продукта, он не попадет в граф автоматически.
- Если вы ХОТИТЕ добавить вспомогательный файл в граф, вам нужно либо изменить правила в `codebase_mapper.py`, либо добавить его вручную.

**Статус:** Граф восстановлен и очищен.
